# 도서비서 (Book Butler) — 프로젝트 기획 문서

> 이 문서는 Claude Code와 ChatGPT Codex가 함께 참고하는 스펙 문서입니다.
> 두 도구 중 어느 쪽에서 작업을 이어가든 이 문서를 먼저 읽고 시작하세요.

---

## 0. 프로젝트 개요

- **프로젝트명**: 도서비서 (가칭, 저장소명 예: `book-butler`)
- **소속**: `동하비서` 워크스페이스 내 프로젝트 (형제 프로젝트: 주식비서, 하루쑥)
- **목적**: 처음엔 개인 사역(성경 읽기/QT 나눔)용으로 만들어 쓰다가, 검증되면 일반 대중 대상 서비스로 확장
- **참고 모델**: 개인 독서 기록 앱 "북스윙(BooksWing)" — 서비스 종료되어 대체 필요
- **개발 협업 도구**: Claude Code(메인) + ChatGPT Codex(보조 크로스체크) + Claude 채팅(기획 문서 보관)

---

## 1. 전체 로드맵 (3단계)

### 1단계 — 개인 기록 (북스윙형) ✅ 최우선 개발 대상
나 혼자 읽고 기록하는 개인 독서 다이어리. 진도, 타임라인, 통계, 인용구 저장.

### 2단계 — 소셜 공유 (인스타형)
사진 찍고 형광펜으로 하이라이트 → 저장 시 글 첨부 가능 → 게시 시점에 **공개 범위 선택** → 매일 인증 스트릭 → 뱃지 시스템 → 카톡/문자로 예쁜 카드 공유.

### 3단계 — 소그룹 커뮤니티
공개 범위를 3단계로 설계해두면 자연 확장:
| 공개 범위 | 설명 |
|---|---|
| 나만 보기 | 비공개 개인 기록 |
| 소그룹 공개 | 특정 그룹원끼리만 보고 댓글 가능 |
| 전체 공개 | 인스타처럼 전체 노출, 좋아요 |

3단계 전용 추가 기능: 그룹 생성/초대, 그룹별 피드 필터링, 댓글 스레드.

**설계 원칙**: 게시물 테이블에 `visibility` 필드를 처음부터 3단계(나만/그룹/전체)로 만들어두면, 나중에 소그룹 기능을 얹을 때 구조를 갈아엎지 않아도 됨.

---

## 2. 1단계 상세 기능 명세 (개발 시작점)

| 기능 | 설명 |
|---|---|
| 책장 (서재) | 읽고 있는/읽은 책 목록, 카테고리별 정리 |
| 진도 기록 | 페이지 슬라이더 + 타이머(읽은 시간 기록) |
| 타임라인 | "○○쪽을 ○○분 동안 읽었습니다" 자동 로그 |
| 인용구/메모 | 페이지 지정 + 문장 저장 + 개인 생각 메모 |
| 통계 | 일별/월별 읽은 쪽수·시간·책 권수 그래프 |
| 한 장의 추억 | 과거 인용구 랜덤 회상 (카톡/문자 공유 가능) |
| **스트릭/뱃지** (1단계부터 선반영) | 연속 읽기 일수 카운터, 30/60/100/200/300일/1년 뱃지, "역대 최장 기록" 대시보드 |

> 뱃지 판정 로직(스트릭 계산, 일수 집계)을 1단계에서 미리 만들어두면, 2단계 소셜 공유 붙일 때 이미 계산된 뱃지를 게시물에 얹기만 하면 됨.

---

## 3. 데이터 구조 (초안)

### 3.1 책 (Book)
```
- id (uuid)
- title, author, publisher, isbn, subtitle, translator
- category (기존 북스윙 63개 카테고리 태그 재사용 가능)
- pages (전체 쪽수), currentPage (현재 쪽수)
- rating (별점)
- status (읽는 중 / 완독 / 위시리스트)
- readCount (완독 횟수)
- startDate, finishDate
```

### 3.2 기록 (Activity/Record) — 책 1권에 여러 개
```
- id (uuid)
- book_id (FK)
- kind: quote(인용구) / quote_with_note(인용구+메모) / photo(사진기록)
        / progress_log(진행로그) / rating(별점기록) / start(읽기시작) / finish(읽기완료)
- text (메모 또는 진행로그 텍스트)
- quote (인용 원문)
- page
- date (timestamp)
- photo (사진 파일 경로/URL)
- visibility (2단계용: private / group / public) ← 처음부터 필드만 만들어둘 것
```

### 3.3 (2단계~) 소셜 관련 테이블
```
- likes: record_id, user_id, reaction_type
- comments: record_id, user_id, text, date
- groups: id, name, member_ids[]
- badges: user_id, badge_type(30/60/100/200/300/365일), earned_date
```

---

## 4. 기존 북스윙 데이터 마이그레이션

### 4.1 백업 데이터 분석 결과
- **형식**: 암호화 없는 순수 JSON (`.book` 파일, 책 1권 = 파일 1개)
- **총 책 수**: 705권
- **총 활동 기록**: 5,666건
- **순수 인용구**: 2,729개
- **촬영 사진**: 768장 (약 102MB)
- **기록 기간**: 2013년 2월 ~ 2026년 9월
- **완독**: 35권 / 읽는 중: 670권
- **카테고리**: 63개 태그 (신앙, 기도, 세계관, 강해, 성경연구 등)

### 4.2 .book 파일 구조 (실제 필드)
```json
{
  "uuid": "...", "title": "...", "author": "...", "publisher": "...",
  "isbn": "...", "category": "신앙", "pages": 103, "currentPage": 1,
  "rating": 5, "status": 1, "readCount": 1, "readDate": 1402284220,
  "activities": [
    {"kind": 2, "text": "인용구 원문", "page": 85, "date": 1402286315},
    {"kind": 1, "photo": "파일명.png", "page": 67, "date": 1402286092},
    {"kind": 0, "quote": "...", "text": "메모", "page": 8, "date": ...},
    {"kind": 4, "quote": "쪽수", "text": "분", "page": ..., "date": ...},
    {"kind": 6, "text": "★★★", "page": 1, "date": ...}
  ]
}
```

**activity kind 매핑표**:
| kind | 의미 |
|---|---|
| 0 | 인용구 + 메모 |
| 1 | 사진 기록 |
| 2 | 순수 인용구 |
| 3 | 읽기 시작 마커 |
| 4 | 진행 로그 (쪽수/시간) |
| 5 | 읽기 완료 마커 |
| 6 | 별점 평가 |
| 7 | 기타(빈 기록/북마크) |

### 4.3 마이그레이션 작업 순서
1. zip 압축 해제 → `.book`(JSON) 파일들 + `_i.png`(표지), `_N.png`(사진) 분리
2. 파싱 스크립트로 위 데이터 구조(3.1, 3.2)에 매핑하여 변환
3. 사진 768장은 별도 스토리지로 업로드, 기록(record)과 URL 연결
4. 새 앱 첫 실행 시 "가져오기" 버튼으로 일괄 이관되도록 설계

---

## 5. 기술 스택 제안

| 단계 | 스택 제안 | 이유 |
|---|---|---|
| 1단계 | Streamlit (주식비서와 동일 패턴) | 책 목록·통계·표 위주라 빠르게 구축 가능, 705권 마이그레이션 데이터 소화 충분 |
| 2단계 | React 기반 웹앱으로 확장 검토 | 사진 위에 형광펜 하이라이트 그리기 등 인터랙티브 이미지 편집 필요 — Streamlit만으론 한계, 이 시점에 재논의 |

---

## 6. 폴더/저장소 구조

```
동하비서/
├── 주식비서/        (기존, GitHub: srapdle-collab/stock-butler)
├── 하루쑥/          (기존)
└── 도서비서/        (신규, GitHub: srapdle-collab/book-butler 가칭)
    ├── data/
    │   └── bookswing_import/     ← 북스윙 백업 원본 zip
    ├── migration/                 ← JSON 파싱 → 새 DB 스키마 변환 스크립트
    ├── app.py                     ← Streamlit 앱 (1단계)
    └── README.md                  ← 이 문서
```

**GitHub 배포 방식**: 주식비서와 동일하게 Private 저장소 + Streamlit Community Cloud(초대 전용)로 배포.

---

## 7. Claude Code / Codex 작업 분담 제안

| 도구 | 역할 |
|---|---|
| Claude Code | 메인 개발 — 저장소 초기 세팅, DB 스키마 구현, 마이그레이션 스크립트, Streamlit 앱 구축 |
| Codex | 서브 — 특정 알고리즘 검토(스트릭 계산 로직 등), Claude Code 결과물 크로스체크, 막힐 때 디버깅 |

**진행 방식**: Claude Code로 큰 흐름을 잡고, 막히는 지점만 Codex에 물어본 뒤 답을 다시 Claude Code에 반영하는 방식 권장 (동시에 같은 코드베이스를 두 도구가 건드리면 컨텍스트 충돌 위험).

---

## 8. 다음 실행 스텝 (체크리스트)

- [ ] `동하비서/도서비서/` 폴더 생성, git 초기화
- [ ] GitHub `book-butler`(가칭) Private 저장소 생성
- [ ] 북스윙 백업 zip을 `data/bookswing_import/`에 저장
- [ ] Claude Code에 이 문서 전달 → 1단계 DB 스키마 + 마이그레이션 스크립트부터 요청 (한 번에 다 시키지 말고 단계별로)
- [ ] 1단계 기본 화면(책장/진도/통계) 완성 후 실제 사용 테스트
- [ ] 안정화되면 2단계(소셜/뱃지/공개범위) 설계 재논의
